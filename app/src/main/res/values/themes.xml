<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GHub" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="android:textSize">16dp</item>

        <item name="android:textColor">@color/black</item>

        <!-- Cor de fundo do botão -->
        <item name="colorPrimary">@color/black</item>

        <!-- Cor de destaque (efeitos e ripple) -->
        <item name="colorPrimaryVariant">@color/gray</item>

        <!-- Cor do texto nos botões -->
        <item name="colorOnPrimary">@android:color/white</item>

        <item name="android:windowBackground">@color/app_background_color</item>


    </style>


    <style name="Theme.GHub" parent="Base.Theme.GHub" />
</resources>